class Flashcard {
  constructor(id, categoryIds, imageUrl, es, en) {
    this.id = id;
    this.categoryIds = categoryIds;
    this.imageUrl = imageUrl;
    this.es = es;
    this.en = en;
  }
}

export default Flashcard;
